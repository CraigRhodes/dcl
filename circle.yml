machine:
 services:
  - docker

dependencies:
 pre:
  - sudo apt-get install make
 override:
 - docker info
 - docker pull craigrhodes/dcl

test:
 override:
  - make -C docker test-ci

deployment:
 prod:
  branch: master
  commands:
   - make -C docker compile-ci
